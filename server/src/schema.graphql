type Query {
    info: String!
    products: [Product!]!
}

input ProductInput{
    title: String!
    price: Float!
}

type Mutation{
    createProduct(product: ProductInput!): Product!
}

type Subscription{
    newProduct: Product
}

type Product{
    id: ID!
    title: String!
    price: Float!
}
